machine:
  timezone:
    Europe/London
  ruby:
    version:
      2.4.2
  environment:
    BASE_DOMAIN: danny.is
dependencies:
  bundler:
    without: [development]
test:
  override:
    - bundle exec rake spec
deployment:
  production:
    branch: master
    heroku:
      appname: dannyis
